### 什么是模块化路由？有什么优势？
模块化路由就是把每个业务功能的接口拆分到独立文件里，再统一挂载到主应用中。
优势：项目结构更清晰、项目更易维护

### 路由
```Python
from fastAPI import APIRouter

#创建APIRouter实例
router=APIRouter(prefix="/api/news",tags=["news"])

@router.get("/categorues")
async def get_categories():
  return {"msg":"获取分类成功"}
```

### 如何注册路由？
在main.py中添加app.include_router()
